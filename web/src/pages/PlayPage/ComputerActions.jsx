import { cards } from 'web/src/pages/PlayPage/PlayPage.jsx'

// Returns the index of the card in playerHand to be played
export const pickComputerCard = (playerHand, oppsHands, round, difficulty) => {
  return 0
}

export const pickComputerSpoon = () => {
  return cards.EGG
}

// ADD FILTER FN
